# dsh-session-search bundle layer: applied automatically when the package is
# installed as a profile bundle (package.json declares dsh.bundle.patch).
- insert:
    - id: dsh-session-search
      name: dsh-session-search
      config: {}
